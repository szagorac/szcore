package com.xenaksys.szcore.event.web.in;

public enum WebScoreInEventType {
    CONNECTION, CONNECTIONS_UPDATE, CONNECTIONS_REMOVE, HELLO, PART_REG, PART_READY, PING, SELECT_ISLOT, SELECT_SECTION,
}
